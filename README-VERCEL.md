# Deploy rápido no Vercel

Este projeto é um app Next.js e pode ser implantado diretamente no Vercel.

**Não coloque este projeto dentro do `public_html` do DirectAdmin.** O DirectAdmin será usado apenas para o fórum/DNS.

Fluxo:

`Vercel → gear.mmonexus.com.br → IPS → iframe /embed`
